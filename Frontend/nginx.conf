server{
    listen: 5173;

    location / {
        root /usr/share/nginx/html;
        index index.html index.html;
        try_files $uri $uri/ /index.html =404;
    }

    include /ect/nginx/extra-conf.d/*.conf;
}